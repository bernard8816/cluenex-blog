# Deployment is handled by Cloudflare Pages automatically on every push to main.
# No GitHub Actions workflow needed — Cloudflare Pages connects directly to this
# repo and runs: npm run build  →  deploys from: docs/
#
# Build settings (configured in Cloudflare Pages dashboard):
#   Framework preset:    None (Eleventy / 11ty)
#   Build command:       npm run build
#   Build output dir:    docs
#   Node.js version:     18
